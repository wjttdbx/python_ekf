# 实验记录：SDRE 追逃博弈中信息结构与博弈范式对性能的影响

> 日期：2026-06-26  
> 场景：a=15000 km, e=0.5, 初距 866 km, 仅测角 EKF (σ_ang=0.008°)

---

## 实验一：双 A 矩阵 — 追逃双方是否必须共享同一个 A_SDC？

### 动机

当前代码中，追逃双方共用同一个 `A_SDC = f(X_p_true, X_e_est)` 求解 ARE 得到 P 矩阵。追方用 EKF 估计状态 x_est，逃方用真实状态 x_true，但 P 矩阵是共用的。理论上逃方通过 `A_SDC(估计) → P` 这条路径间接受到滤波误差的影响。

### 四种模式

| 模式 | A 矩阵来源 | 状态反馈 |
|------|-----------|---------|
| 全知 (上界) | A_true (双方) | 双方都用真实状态 |
| shared_A (现状) | A_est (双方) | 追方 x_est，逃方 x_true |
| dual_A (实验) | 追方 A_est → P_p，逃方 A_true → P_e | 同上 |
| true_A (对照) | A_true (双方，但追方状态仍是 x_est) | 追方 x_est，逃方 x_true |

### 结果

| 模式 | 捕获时间 | 全知是 baseline |
|------|----------|----------------|
| 全知 | 24.48 h | — |
| shared_A | 15.34 h | −37% |
| dual_A | 15.30 h | −38% |
| true_A | 15.34 h | −37% |

**结论**：用不同的 A 矩阵（dual_A）可以追到，性能与共享 A（shared_A）几乎相同。原因是 A_SDC 对相对状态估计误差不敏感——绝对地心距离 r_p, r_e 主导 SDC 矩阵。||P_p − P_e||_F 最大到 4.5×10⁸ 但不改变反馈增益的符号和方向。SDRE 的滚动时域特性天然容忍单步模型失配。

**反直觉发现**：三种 EKF 模式（~15h）都比全知 Nash 鞍点（24.5h）快。完美的 Nash 均衡反而是最慢的。

### 文件

- 脚本：`experiment_dual_a_matrix.py`
- 图片：`outputs/figures/dual_a_matrix_comparison.png`
- 数据：`outputs/data/dual_a_matrix/`

---

## 实验二：替代博弈理论 — 能否超越 SDRE Nash 鞍点？

### 动机

SDRE Nash 解假设完美信息、无限时域、双方同时最优。现实中追方只有 EKF 估计，逃方策略固定。偏离鞍点的策略可能更好。

### 六种博弈范式

| 范式 | 机制 |
|------|------|
| Nash 基线 | γ_p=γ_e=√2, 共用 A_est |
| **Hypergame γ_p=2.0** | 追方暗中用 γ_p=2.0 (更激进)，逃方以为 γ=√2 |
| **Hypergame γ_p=3.0** | 追方 γ_p=3.0 |
| H∞ 鲁棒 | 3σ worst-case 偏置 |
| 信念自适应 R α=0.5 | R = R₀·(tr(P)/tr(P₀))^0.5 |
| 信念自适应 R α=1.0 | R = R₀·(tr(P)/tr(P₀))^1.0 |

### 结果

| 范式 | 捕获时间 | vs Nash |
|------|----------|---------|
| Nash 基线 | 15.34 h | — |
| Hypergame γ_p=2.0 | 11.59 h | −24.5% |
| **Hypergame γ_p=3.0** | **10.41 h** | **−32.1%** |
| H∞ 鲁棒 | 11.59 h | −24.4% |
| 信念自适应 R α=1.0 | 10.98 h | −28.4% |
| 信念自适应 R α=0.5 | 13.50 h | −12.0% |

**推力分析**：所有模式的最大推力几乎相同（max u_p ≈ 1.9×10⁻³ km/s²），区别在**平均推力**。改进方法把推力预算集中在前中期，在逃方拉开距离前快速逼近。不是"更大"，是"更持久"。

**结论**：Hypergame γ_p=3.0 是最有效的简单改进——改一行参数（γ_p），零额外计算量，快 32%。Nash 均衡是"双方都最优"的固定点，但追方的最优回应不是 Nash，而是对固定逃方策略的 best response。

### 文件

- 脚本：`experiment_game_theory_compare.py`
- 图片：`outputs/figures/game_theory_comparison.png`
- 数据：`outputs/data/game_theory_comparison/`

---

## 实验三：性能极限探索 — γ_p 扫描

### 动机

实验二发现 γ_p 越大越好。是否存在性能饱和点？

### γ_p 扫描

```
γ_p = √2 (Nash)  →  15.33 h
γ_p = 1.5        →  14.23 h   (−7%)
γ_p = 2.0        →  11.59 h  (−24%)
γ_p = 3.0        →  10.41 h  (−32%)
γ_p = 5.0        →   9.81 h  (−36%)
γ_p = 10.0       →   8.98 h  (−41%)
γ_p = 50.0       →   8.90 h  (−42%)
γ_p = 100.0      →   8.87 h  (−42%)   ← 饱和
```

**结论**：性能在 γ_p ≈ 10 处饱和，极限约 **8.9 小时**。R_eff_p → R 后物理推力上限成为瓶颈。比 Nash 快 42%，比全知 Nash 快 64%（全知 = 24.48 h）。

有限时域 MPC 尝试因 ARE 病态而发散，未完成验证。

### 文件

- 脚本：`experiment_performance_limit.py`
- 图片：`outputs/figures/performance_limit.png`
- 数据：`outputs/data/performance_limit/`

---

## 实验四：观测模型对比 — Doppler 距变率测量的价值

### 动机

当前 EKF 只用位置测量（ρ, az, el），速度仅通过动力学间接传播。引入 Doppler（距变率 ρ̇）能否改善性能？

### EKF 扩展

在 `RelativeStateEKF` 中新增 `use_doppler` 参数：

- 新方法 `measure_range_rate(x_rel)`：ρ̇ = (r·v)/ρ
- 扩展 `measure()` / `meas_jacobian()`：追加 ρ̇ 行
- Jacobian 速度分量不再为零：∂ρ̇/∂v_i = r_i/ρ

### 四种观测模式

| 模式 | 测量向量 | 位置可观？ | 速度可观？ |
|------|----------|-----------|-----------|
| AO | [az, el] | 弱 (动力学间接) | ✗ |
| RA | [ρ, az, el] | ✓ | ✗ |
| AD | [az, el, ρ̇] | 弱 | ✓ |
| FULL | [ρ, az, el, ρ̇] | ✓ | ✓ |

### 结果（均用 Hypergame γ_p=3.0）

| 模式 | 捕获时间 | 最终位置误差 | P_trace | 不确定性 |
|------|----------|-------------|---------|---------|
| **AO** | **10.41 h** | 33 m | 5.33 | **高 (从未收敛)** |
| RA | 17.18 h | 7 m | 8.8e-5 | 低 |
| AD | 18.52 h | 684 m | 1.11 | 中 |
| FULL | 17.19 h | 3 m | 8.8e-5 | 低 |

**核心矛盾**：更多信息 → 更精确估计 → 更接近 Nash → **更慢的捕获**。

AO 的 P_trace 始终很大（5.33），EKF 从未收敛，但估计均值意外地好（33m）。这种持续的不确定性维持了控制中的有益扰动，打破了 Nash 对称性。RA 和 FULL 协方差降到 8.8e-5，估计精确到米级，但控制回到了保守模式，捕获时间从 10.4h 暴增到 17.2h。

AD（角度+Doppler）是四种中最差方案。Doppler 给了速度信息但缺少距离 → 位置消歧失败 → 684m 误差 → 最慢捕获。**Doppler 必须和测距配合才有价值。**

### 文件

- EKF 修改：`aerospace/estimation/ekf.py`
- 脚本：`experiment_obs_model_compare.py`
- 图片：`outputs/figures/obs_model_comparison.png`
- 数据：`outputs/data/obs_model_comparison/`

---

## 总览：性能对比全景

```
                    全知 Nash                               24.48 h  ← 理论上界
                    ─────────────────────────────────────
                    
Nash 基线 (AO)                                         15.34 h  ← 当前基线
Hypergame γ_p=3.0 (AO)                          10.41 h  ← 最好 (零代价)
Hypergame γ_p=100 (AO)                       8.87 h  ← 饱和极限
                    
RA (距离+角度)                              17.18 h  ← 精确但慢
AD (角度+Doppler)                             18.52 h  ← 最差
FULL (全观测)                                17.19 h  ← 精确但慢
```

## 核心发现

1. **SDRE Nash 不是"追捕问题"的最优解**。它是对称信息、无限时域下的最优，但追方有信息劣势且面对固定策略的逃方时，偏离 Nash 可以获益 32~42%。

2. **A 矩阵共享不是瓶颈**。估计误差对 SDC 矩阵影响微弱，双 A 矩阵（各自求解 ARE）在工程上等价于共享 A。

3. **更多测量 ≠ 更好性能**。精确的 EKF 估计让控制回到保守的 Nash 模式，反而减慢捕获。观测精度和博弈激进程度之间存在 trade-off。

4. **最大推力不是区分因素**。所有策略的推力峰值几乎相同（~1.9e-3 km/s²），改进来自推力时间分配的优化。

5. **Hypergame 是最优 Pareto 改进**。改一个参数（γ_p=2~3），零计算代价，24~32% 加速。理论上是 Bayesian game with private type 的 best response 策略。
