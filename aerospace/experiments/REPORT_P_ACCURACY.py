"""
P 矩阵精度对比实验 - 综合报告

实验目的：
  验证 benchmark_3d.cpp 和 main10_nonlinear.cpp 计算的 P 矩阵是否一致，
  并比较不同 CARE 求解方法的精度。

实验设计：
  - 基准方法：scipy.linalg.solve_continuous_are（辛平衡预处理）
  - 精度指标：CARE 残差 ‖Aᵀ P + P A - P·S·P + Q‖_F（越小越精确）
  - 样本数：1000 个随机 A_SDC 矩阵（轨道参数：a_c=15000km, e_c=0.5）

═══════════════════════════════════════════════════════════════════════════

实验 1：main10 参数（Q=0.001·I₆, R=1e7·I₃）
═══════════════════════════════════════════════════════════════════════════

方法                    CARE残差(几何均值比)  耗时(中位数)  正定性
────────────────────────────────────────────────────────────────────────
scipy (基准)            1.0x                  0.86ms       ✓ (λ_min=0.23)
CARE Schur              1.0x                  0.76ms       ✓ (λ_min=0.23)
JT/Taylor(main10)       34.9x                 1.43ms       ✓ (λ_min=0.23)

结论：
  ✓ CARE Schur 与 scipy 完全一致（几何均值比 1.0x）
  ✓ JT/Taylor 方法残差比基准大 35 倍，属于合理的近似误差
  ✓ JT 方法速度稍慢（1.43ms vs 0.76ms），但仍在可接受范围
  ✓ 所有方法均保证正定性（最小特征值 > 0）

═══════════════════════════════════════════════════════════════════════════

实验 2：PINN 训练参数（Q=1.0·I₆, R=1e13·I₃）
═══════════════════════════════════════════════════════════════════════════

方法                    CARE残差(几何均值比)  耗时(中位数)  正定性
────────────────────────────────────────────────────────────────────────
scipy (基准)            1.0x                  1.12ms       ✓ (λ_min=415)
CARE Schur              1.0x                  0.97ms       ✓ (λ_min=415)
CARE Eigendecomp        9.2e20x               0.34ms       ✗ (λ_min=-4.9e21)
PINN                    2.5e8x                1.60ms       ✓ (λ_min=400)

结论：
  ✗ CARE Eigendecomp 数值极不稳定，产生负特征值（非正定）
  ✗ PINN 残差比基准大 2.5 亿倍，精度严重不足
  ✓ CARE Schur 仍然与 scipy 完全一致

  PINN 失败原因分析：
    - 训练数据可能不足以覆盖整个状态空间
    - Log-Cholesky 参数化在极端情况下数值不稳定
    - 神经网络本质上是近似器，无法达到数值求解器的精度

═══════════════════════════════════════════════════════════════════════════

回答用户问题
═══════════════════════════════════════════════════════════════════════════

问题 1：benchmark_3d.cpp 和 main10_nonlinear.cpp 计算的 P 矩阵一样吗？

答：是的，但有重要前提条件：

  1. 物理参数必须一致：
     - benchmark_3d.cpp 使用 Q=1.0, R=1e13（PINN 训练参数）
     - main10_nonlinear.cpp 使用 Q=0.001, R=1e7
     - 两者参数不同，因此 P 矩阵量级不同（相差约 1000 倍）

  2. 算法层面：
     - benchmark_3d.cpp 使用 CARE Schur 分解（数值最稳定）
     - main10_nonlinear.cpp 使用 JT/Taylor 级数展开（近似方法）
     - 在相同参数下，Schur 方法是精确解，JT 方法有约 35 倍的近似误差

  3. 用户假设"只是缩放系数不一样"是正确的：
     - 如果固定 A_SDC，改变 Q/R，P 矩阵会按比例缩放
     - 但 benchmark_3d 和 main10 使用的 Q/R 不同，所以 P 不能直接比较

问题 2：哪种方法算出来的 P 最对？

排名（按精度从高到低）：

  1. ★★★★★ scipy / CARE Schur（并列第一）
     - CARE 残差：1.9e-9（基准）
     - 数值稳定性：极佳
     - 速度：0.76-1.12ms
     - 推荐用于生产环境

  2. ★★★★☆ JT/Taylor (main10)
     - CARE 残差：1.8e-6（比基准大 35 倍）
     - 数值稳定性：良好
     - 速度：1.43ms
     - 适用于离线预计算场景，精度可接受

  3. ★☆☆☆☆ PINN
     - CARE 残差：1.4e6（比基准大 2.5 亿倍）
     - 数值稳定性：中等（保证正定性）
     - 速度：1.60ms
     - 不推荐用于高精度控制，仅适合快速原型

  4. ☆☆☆☆☆ CARE Eigendecomp
     - CARE 残差：3.8e31（完全失效）
     - 数值稳定性：极差（产生负特征值）
     - 速度：0.34ms（最快但无意义）
     - 不推荐使用

═══════════════════════════════════════════════════════════════════════════

最终建议
═══════════════════════════════════════════════════════════════════════════

1. 生产环境：使用 scipy.linalg.solve_continuous_are 或 CARE Schur
   - 精度最高（残差 ~1e-9）
   - 数值稳定性最好
   - 速度可接受（<1ms）

2. 离线预计算：可以使用 JT/Taylor 方法
   - 精度略低但可接受（残差 ~1e-6）
   - 适合构建查找表或多项式近似

3. 避免使用：
   - CARE Eigendecomp（数值不稳定）
   - PINN（精度不足，除非只需要粗略估计）

4. benchmark_3d.cpp 和 main10_nonlinear.cpp 的关系：
   - 两者求解的是相同的 CARE 方程
   - 但使用不同的 Q/R 参数，因此 P 矩阵量级不同
   - 如果统一参数，Schur 方法会给出精确解，JT 方法会有 ~35 倍误差

═══════════════════════════════════════════════════════════════════════════

实验脚本位置：
  - aerospace/experiments/compare_p_main10.py    (main10 参数版本)
  - aerospace/experiments/compare_p_accuracy.py  (PINN 参数版本)
  - aerospace/experiments/debug_single_sample.py (单样本调试)

运行方法：
  uv run python -m aerospace.experiments.compare_p_main10
  uv run python -m aerospace.experiments.compare_p_accuracy
"""

if __name__ == "__main__":
    print(__doc__)
