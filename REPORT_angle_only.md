# 仅测角 EKF-SDRE 为何比理想 SDRE 更早进入捕获圈

## 一、结论先行

仅测角 EKF-SDRE 并没有在相同意义上“优于”理想 SDRE。它只是在当前指标下更早第一次进入 `rho < 100 m` 的捕获圈。

原因是三件事叠加：

1. **SDRE 优化的不是捕获时间**。当前代价函数没有显式的时间项和终端会合速度约束，所以全状态 SDRE 不必是最短时间策略。
2. **仅测角的不可观方向同时污染了位置和速度估计**。控制律 `u_p = -R^-1 B^T P(A) x_hat` 直接使用估计状态。十种子因果干预表明，在本 2D 场景中，**速度估计误差是主导通道，距离高估只是次要通道**。
3. **判定条件是首次进圈，不是软会合**。仅测角组以更高的终端相对速度穿过 100 m 边界，因此首达时间更短。

“峰值推力相同、燃料相近”不能决定捕获时间：推力的方向、时序和制动程度不同，完全可以在几乎相同的 Delta-V 下得到不同的首达时间。

## 二、基准现象

> 2D `scenario_2d_1`，10 种子均值，baseline 组

| 指标 | 仅测角 EKF | 距离+角度 EKF | 理想 SDRE（无 EKF、无噪声） |
|------|-----------|-------------|------------------------|
| 首次进入 100 m | **5.13 +/- 0.16 h** | 8.12 +/- 0.01 h | 7.99 +/- 0.00 h |
| 控制平方积分 `integral ||u_p||^2 dt` | 2.99e-4 | 3.01e-4 | 2.98e-4 |
| 推力抖动 `mean ||Delta u||` | 1.07e-6 | 7.60e-7 | **6.55e-7** |

注意：`integral ||u||^2 dt` 是控制能量指标，**不是燃料消耗**。对于恒定比冲的理想推进系统，更接近燃料的量是 `Delta-V = integral ||u|| dt`。本轮重算 `seed=0` 得到：

| 指标 | 仅测角 EKF | 距离+角度 EKF | 理想 SDRE |
|------|-----------:|-------------:|----------:|
| Delta-V (km/s) | 1.380 | 1.408 | 1.390 |
| 峰值推力 (km/s^2) | 3.788e-4 | 3.774e-4 | 3.787e-4 |
| 首次进圈相对速度 (m/s) | **0.307** | 0.095 | 0.088 |

因此，用户观察到的“最大推力和燃料消耗不变差”基本成立，但不会推出捕获时间必须相同。

## 三、单种子轨迹诊断

> `seed=0`

| 指标 | 仅测角 EKF | 距离+角度 EKF | 理想 SDRE |
|------|-----------|-------------|----------|
| 平均距离估计偏差 | **+4.08 km**（97% 时间高估） | 约 0 km | - |
| 平均推力 `integral ||u||dt / T` | **7.67e-5** | 4.82e-5 | 4.83e-5 |
| 高推力占比（>75% 峰值） | **10.2%** | 6.5% | 6.4% |
| 首次进圈时间 | **4.994 h** | 8.114 h | 7.992 h |
| 首次进圈相对速度 | **0.307 m/s** | 0.095 m/s | 0.088 m/s |

仅测角组的平均推力更大，主要是相近 Delta-V 被压缩到更短的轨迹时间中；它不等价于更大的总控制投入。

## 四、因果干预：到底是哪个估计误差在起作用

为避免只凭相关性归因，在每个控制步对控制器输入做反事实替换。EKF 本身仍正常运行，只把某一个通道替换为真值。下表为 10 种子均值：

| 干预 | 首次进入 100 m | Delta-V (km/s) | 控制平方积分 | 进圈相对速度 (m/s) | 解读 |
|------|----------------:|---------------:|------------------:|-----------------------:|------|
| 仅测角 baseline | **5.128 +/- 0.158 h** | 1.383 | 2.992e-4 | 0.216 +/- 0.135 | 原始现象 |
| 仅把 `A_SDC` 换成真值 | **5.128 +/- 0.159 h** | 1.383 | 2.992e-4 | 0.217 +/- 0.135 | `A_SDC -> P` 通道在本场景几乎无贡献 |
| 仅纠正距离 | 5.954 +/- 0.245 h | 1.408 | 2.984e-4 | 0.204 +/- 0.155 | 距离高估有贡献，但不是全部 |
| 将位置估计全部换成真值 | 5.957 +/- 0.245 h | 1.408 | 2.984e-4 | 0.203 +/- 0.153 | 与“仅纠正距离”几乎一样 |
| 将速度估计换成真值 | **9.463 +/- 0.041 h** | 1.502 | 3.043e-4 | 0.082 +/- 0.010 | 加速优势完全消失，速度通道主导 |
| 将全部控制状态换成真值 | **7.992 +/- 0.000 h** | 1.390 | 2.977e-4 | 0.088 +/- 0.000 | 恢复理想 SDRE |

这些干预不能做简单加性分解，因为一个通道改变后，整条闭环轨迹也改变。但它们足以排序主要因果通道：

`velocity-estimation error >> range/position error >> A_SDC perturbation`

对 `seed=0` 进一步将速度误差分解到径向和横向：仅纠正径向速度时捕获时间为 5.347 h，仅纠正横向速度时为 9.061 h。因此，本场景中最关键的是**横向速度估计误差改变了推力方向和制动时序**，而不是单纯的“距离看得更远，所以推得更大”。

## 五、为什么相同 Delta-V 可以更快

Delta-V 和峰值推力都是标量汇总，它们丢失了推力方向和时序信息。两条轨迹可以消耗相同 Delta-V，但：

- 一条轨迹把更多控制用于减小横向速度和终端制动，进圈更慢、终端速度更低；
- 另一条轨迹在错误的速度估计下减少或延后制动，以更高的闭合速度提前穿过 100 m 边界。

`seed=0` 继续积分而不在第一次进圈时停止，得到：

- 仅测角首次 `rho < 100 m`：4.994 h，当时 `v_rel = 0.307 m/s`；
- 再加 `v_rel < 0.1 m/s` 的软捕获条件：5.675 h；
- 理想 SDRE 在 7.992 h 首次进圈时已同时满足 `v_rel < 0.1 m/s`。

这说明捕获圈首达偏差确实放大了时间优势，但在 `seed=0` 中并非全部原因：即使加入软捕获速度条件，仅测角仍在 5.675 h 达标。不过，长时间继续仿真中该闭环会再次远离，所以还不能把“首次达标”解释为稳定会合。

## 六、代价函数的正确解读

> `J = integral (||x||^2 + R||u_p||^2 - gamma^2 R||u_e||^2) dt`，`seed=0`

| 分项 | 仅测角 EKF | 距离+角度 EKF | 理想 SDRE |
|------|-----------|-------------|----------|
| `J_total` | 2.2901e9 | 2.2903e9 | 2.2837e9 |
| `J_state` | 7.76e8 | 8.06e8 | 7.95e8 |
| `J_ctrl_net` | 1.514e9 | 1.484e9 | 1.488e9 |

`J_total` 差异小于 0.3%，但这**不能证明仅测角和理想 SDRE 同样最优**：

- 三组积分的终止时间不同，不是同一有限时域代价的严格对比；
- 当前 `J` 没有显式的捕获时间惩罚，也没有终端相对速度惩罚；
- SDRE 是沿当前 SDC 参数化求局部 ARE 反馈，不是该非线性最短时间问题的全局解。

因此，更准确的说法是：**仅测角估计误差把闭环推离了全状态 SDRE 轨迹，在几乎相同的汇总控制代价下，偶然得到了更短的首达时间。**

## 七、参数敏感度

| 扫描参数 | 仅测角 EKF | 距离+角度 EKF | 解读 |
|---------|-----------|-------------|------|
| `sigma_ang = 0.001 deg` | 7.52 h | 8.12 h | 估计误差很小，接近全状态轨迹 |
| `sigma_ang = 0.008 deg` | 5.13 h | 8.12 h | 基准 |
| `sigma_ang = 0.05 deg` | **3.64 h** | 8.12 h | 更大状态估计误差引起更大的轨迹偏移 |
| `e = 0` | 3.30 h | 6.81 h | 效应不需要 NERM 强非线性 |
| `e = 0.5` | 5.13 h | 8.12 h | 基准 |
| `e = 0.7` | 6.87 h | 10.02 h | 高偏心率下效应减弱 |
| `gamma = 1.1` | 8.31 h | 18.30 h | 对博弈参数敏感 |
| `gamma = 1.41` | 5.13 h | 8.12 h | 基准 |
| `gamma = 5.0` | 3.88 h | 5.32 h | 逃逸作用弱化后差距缩小 |

这些扫描只能说明现象对估计误差和博弈参数敏感，不能单独证明“距离高估”是根因。

## 八、3D 对照（历史结果，本轮未重跑）

| 指标 | 仅测角 EKF | 距离+角度 EKF | 理想 SDRE |
|------|-----------|-------------|----------|
| 首次进圈时间 | **3.68 h** | 5.29 h | 5.16 h |
| 控制平方积分 | 3.77e-6 | 4.54e-6 | 4.17e-6 |

3D 结果与 2D 在“仅测角首达更早”上定性一致，但不能直接假定 2D 的速度通道排序在 3D 中也完全一样；3D 仍需要做同样的逐通道因果干预。

## 九、代码中的闭环路径

本现象可以在代码中沿以下路径定位：

1. `aerospace/estimation/ekf_2d.py:35-69`：仅测角测量只有 `theta`，没有距离通道；速度也只能通过动力学和视线时序间接校正。
2. `aerospace/simulation/nerm_ekf_sdre_2d.py:131-145`：有噪声时 `x_ctrl = self.ekf.x`，估计位置和估计速度被直接送入 SDRE。
3. `aerospace/control/sdre_2d.py:147-149`：控制量是 `P @ x_rel` 的线性反馈，因此速度估计误差会直接改变推力方向和制动量。
4. `aerospace/simulation/nerm_ekf_sdre_2d.py:177-181`：仿真在距离小于 100 m 时立即终止，没有检查相对速度或驻留时间。

## 十、最终结论

1. **“更快”只指首次进入 100 m 更早**，不代表估计更准、代价更低或会合更稳定。
2. **同样的峰值推力和近似相同的 Delta-V 完全可以产生不同的首达时间**，因为推力方向、时序和终端制动不同。
3. **本 2D 场景的主因是仅测角 EKF 的速度估计误差，尤其是横向速度通道**。距离高估有贡献，但不是充分解释。
4. **`A_SDC` 因估计误差而改变的通道在本场景可忽略**：强制使用真值 `A_SDC` 后捕获时间不变。
5. **理想 SDRE 不是最短时间基准**。要比较“谁真正更优”，必须先统一任务目标，例如同时约束 `rho < 100 m`、`v_rel < 0.1 m/s` 和驻留时间，再在相同固定时域上比较 Delta-V 与代价函数。
